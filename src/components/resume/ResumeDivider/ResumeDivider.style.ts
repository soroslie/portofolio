import styled from 'styled-components'
import ColorPalletes from '../../../utils/color'

export const Container = styled.div`
  height: 1px;
  width: 100%;
  background-color: ${ColorPalletes.blackPrimary};
  margin: 0.2rem 0 0.2rem 0;
`
